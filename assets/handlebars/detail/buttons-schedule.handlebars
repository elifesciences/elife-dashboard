<button class="btn btn-default schedule schedule-btn" id="schedule-{{ article.id }}" data-action-type="schedule" data-toggle="modal" data-target="#schedule-modal"  data-title="Schedule Article"
        data-article-id="{{ article.id }}">
    <span class="fa fa-calendar"></span>
    Schedule
</button>